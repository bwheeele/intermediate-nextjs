import type * as llamaindex from "llamaindex";
import { InstrumentationBase, InstrumentationModuleDefinition } from "@opentelemetry/instrumentation";
import { LlamaIndexInstrumentationConfig } from "./types";
export declare class LlamaIndexInstrumentation extends InstrumentationBase {
    protected _config: LlamaIndexInstrumentationConfig;
    constructor(config?: LlamaIndexInstrumentationConfig);
    setConfig(config?: LlamaIndexInstrumentationConfig): void;
    manuallyInstrument(module: typeof llamaindex): void;
    protected init(): InstrumentationModuleDefinition;
    private isLLM;
    private isEmbedding;
    private isSynthesizer;
    private isRetriever;
    private patch;
    private unpatch;
}
//# sourceMappingURL=instrumentation.d.ts.map
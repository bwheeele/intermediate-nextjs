/**
 * Laminar Claude Code Proxy - TypeScript/JavaScript bindings for HTTP proxy server
 */
export interface SpanContext {
    traceId: string;
    spanId: string;
    projectApiKey: string;
    spanIdsPath?: string[];
    spanPath?: string[];
    laminarUrl?: string;
}
/**
 * A Claude Code Proxy server instance that can be run on a specific port.
 *
 * Each instance manages its own server state, health monitoring,
 * and can be started/stopped independently.
 *
 * @example
 * ```typescript
 * // Create multiple proxy instances on different ports
 * const proxy1 = new ProxyServer(45667);
 * const proxy2 = new ProxyServer(45668);
 *
 * proxy1.runServer('https://api.anthropic.com');
 * proxy2.runServer('https://api.anthropic.com');
 *
 * await proxy1.setCurrentTrace({ traceId: '...', spanId: '...', projectApiKey: '...' });
 * await proxy2.setCurrentTrace({ traceId: '...', spanId: '...', projectApiKey: '...' });
 *
 * proxy1.stopServer();
 * proxy2.stopServer();
 * ```
 */
export declare class ProxyServer {
    private port;
    private targetUrl;
    private monitorInterval;
    private isHealthCheckRunning;
    private _isRunning;
    /**
     * Initialize a new proxy server instance.
     *
     * @param port - The port to listen on (default: 45667)
     */
    constructor(port?: number);
    /**
     * Set the current trace context by sending an HTTP request to this proxy server.
     *
     * @param context - The span context containing trace information
     * @throws Error if the HTTP request fails
     */
    setCurrentTrace(context: SpanContext): Promise<void>;
    /**
     * Background function that monitors server health and restarts if needed.
     * Polls every HEALTH_CHECK_INTERVAL ms.
     * Uses a flag to prevent concurrent execution, avoiding race conditions.
     */
    private healthCheckMonitor;
    /**
     * Run the proxy server in a background thread with health monitoring.
     *
     * A background monitor will check the server health every HEALTH_CHECK_INTERVAL ms
     * and automatically restart it if it dies.
     *
     * @param url - The target URL to proxy requests to
     */
    runServer(url: string): void;
    /**
     * Stop the proxy server and its health monitoring.
     *
     * @param timeout - Maximum time in seconds to wait for graceful shutdown (default: 4).
     *                  The server will abort pending connections and background tasks after timeout/2.
     */
    stopServer(timeout?: number): void;
    /**
     * Check if the server is currently running.
     */
    get isRunning(): boolean;
    /**
     * Get the port this server is running on.
     */
    getPort(): number;
}
/**
 * Run the proxy server in a background thread with health monitoring.
 *
 * This is a backwards-compatible function that uses a global singleton instance.
 * For more control, consider creating your own ProxyServer instance.
 *
 * A background monitor will check the server health every HEALTH_CHECK_INTERVAL ms
 * and automatically restart it if it dies.
 *
 * @param url - The target URL to proxy requests to
 * @param port - The port to listen on (default: 45667)
 */
export declare function runServer(url: string, port?: number): void;
/**
 * Stop the proxy server and its health monitoring.
 *
 * This is a backwards-compatible function that uses a global singleton instance.
 * For more control, consider creating your own ProxyServer instance.
 *
 * @param timeout - Maximum time in seconds to wait for graceful shutdown (default: 4).
 *                  The server will abort pending connections and background tasks after timeout/2.
 */
export declare function stopServer(timeout?: number): void;
/**
 * Set the current trace context by sending an HTTP request to the running proxy server.
 *
 * This is a backwards-compatible function that uses a global singleton instance.
 * For more control, consider creating your own ProxyServer instance.
 *
 * @param context - The span context containing trace information
 * @throws Error if the HTTP request fails
 */
export declare function setCurrentTrace(context: SpanContext): Promise<void>;
export declare const run: any, stop: any, stopWithTimeout: any;

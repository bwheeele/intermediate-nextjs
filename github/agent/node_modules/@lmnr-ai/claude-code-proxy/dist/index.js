"use strict";
/**
 * Laminar Claude Code Proxy - TypeScript/JavaScript bindings for HTTP proxy server
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.stopWithTimeout = exports.stop = exports.run = exports.ProxyServer = void 0;
exports.runServer = runServer;
exports.stopServer = stopServer;
exports.setCurrentTrace = setCurrentTrace;
// Platform-specific package loading
// Uses explicit .node subpath exports for bundler compatibility=
const { join } = require('path');
const { platform, arch } = require('os');
function loadNative() {
    // Map of platform/arch to NAPI platform names
    const platformMap = {
        'darwin-arm64': 'darwin-arm64',
        'darwin-x64': 'darwin-x64',
        'win32-x64': 'win32-x64-msvc',
        'linux-x64': process.env.MUSL ? 'linux-x64-musl' : 'linux-x64-gnu',
        'linux-arm64': process.env.MUSL ? 'linux-arm64-musl' : 'linux-arm64-gnu',
    };
    const platformKey = `${platform()}-${arch()}`;
    const platformName = platformMap[platformKey];
    // Try multiple paths - the explicit .node extension tells bundlers
    // this is a native module and should be left external
    const paths = [
        // Development build path (local .node file)
        join(__dirname, 'lmnr-claude-code-proxy.node'),
        // Platform package path with explicit .node subpath (bundler-friendly)
        // The /lmnr-claude-code-proxy.node subpath is resolved via package.json exports
        `@lmnr-ai/claude-code-proxy-${platformName}/lmnr-claude-code-proxy.node`,
    ];
    const errors = [];
    for (const path of paths) {
        try {
            return require(path);
        }
        catch (e) {
            errors.push(e);
        }
    }
    throw new Error(`Failed to load native module for ${platformKey}. ` +
        `Platform package: @lmnr-ai/claude-code-proxy-${platformName || 'unknown'}. ` +
        `Tried paths: ${paths.join(', ')}. ` +
        `Errors: ${errors.map(e => e.message).join('; ')}`);
}
const native = loadNative();
const HEALTH_CHECK_INTERVAL = 1000;
/**
 * A Claude Code Proxy server instance that can be run on a specific port.
 *
 * Each instance manages its own server state, health monitoring,
 * and can be started/stopped independently.
 *
 * @example
 * ```typescript
 * // Create multiple proxy instances on different ports
 * const proxy1 = new ProxyServer(45667);
 * const proxy2 = new ProxyServer(45668);
 *
 * proxy1.runServer('https://api.anthropic.com');
 * proxy2.runServer('https://api.anthropic.com');
 *
 * await proxy1.setCurrentTrace({ traceId: '...', spanId: '...', projectApiKey: '...' });
 * await proxy2.setCurrentTrace({ traceId: '...', spanId: '...', projectApiKey: '...' });
 *
 * proxy1.stopServer();
 * proxy2.stopServer();
 * ```
 */
class ProxyServer {
    /**
     * Initialize a new proxy server instance.
     *
     * @param port - The port to listen on (default: 45667)
     */
    constructor(port = 45667) {
        this.targetUrl = '';
        this.monitorInterval = null;
        this.isHealthCheckRunning = false;
        this._isRunning = false;
        this.port = port;
    }
    /**
     * Set the current trace context by sending an HTTP request to this proxy server.
     *
     * @param context - The span context containing trace information
     * @throws Error if the HTTP request fails
     */
    async setCurrentTrace(context) {
        const { traceId, spanId, projectApiKey, spanIdsPath = [], spanPath = [], laminarUrl = 'https://api.lmnr.ai', } = context;
        // Prepare the JSON payload
        const payload = {
            trace_id: traceId,
            span_id: spanId,
            project_api_key: projectApiKey,
            span_ids_path: spanIdsPath,
            span_path: spanPath,
            laminar_url: laminarUrl,
        };
        // Send POST request to the internal endpoint
        const url = `http://127.0.0.1:${this.port}/lmnr-internal/span-context`;
        try {
            const response = await fetch(url, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify(payload),
            });
            if (!response.ok) {
                throw new Error(`HTTP ${response.status}`);
            }
        }
        catch (error) {
            throw new Error(`Failed to set trace context: ${error}`);
        }
    }
    /**
     * Background function that monitors server health and restarts if needed.
     * Polls every HEALTH_CHECK_INTERVAL ms.
     * Uses a flag to prevent concurrent execution, avoiding race conditions.
     */
    async healthCheckMonitor() {
        // Skip if a health check is already running to prevent concurrent restarts
        if (this.isHealthCheckRunning) {
            return;
        }
        this.isHealthCheckRunning = true;
        try {
            const url = `http://127.0.0.1:${this.port}/lmnr-internal/health`;
            try {
                const response = await fetch(url, {
                    // Force new connection for each health check to avoid interfering with streaming
                    headers: {
                        'Connection': 'close'
                    },
                });
                if (response.status === 200) {
                    // Server is healthy
                    return;
                }
            }
            catch (error) {
                // Server is not responding, try to restart
                console.warn(`Server health check failed on port ${this.port}: ${error}. Attempting to restart...`);
                try {
                    // Try to stop the existing server (it might be stuck)
                    try {
                        native.stopWithTimeout(this.port, 4);
                    }
                    catch {
                        // Server might already be dead
                    }
                    // Wait a bit before restarting
                    await new Promise(resolve => setTimeout(resolve, 100));
                    // Restart the server
                    native.run(this.targetUrl, this.port);
                }
                catch (restartError) {
                    console.error(`Failed to restart server on port ${this.port}: ${restartError}`);
                }
            }
        }
        finally {
            // Always reset the flag when done, even if an error occurred
            this.isHealthCheckRunning = false;
        }
    }
    /**
     * Run the proxy server in a background thread with health monitoring.
     *
     * A background monitor will check the server health every HEALTH_CHECK_INTERVAL ms
     * and automatically restart it if it dies.
     *
     * @param url - The target URL to proxy requests to
     */
    runServer(url) {
        // Stop any existing monitoring
        if (this.monitorInterval !== null) {
            clearInterval(this.monitorInterval);
            this.monitorInterval = null;
        }
        // Store server configuration
        this.targetUrl = url;
        // Start the server
        try {
            native.run(url, this.port);
            this._isRunning = true;
        }
        catch (error) {
            console.error(`Error running the proxy server on port ${this.port}: ${error}`);
            throw error;
        }
        // Start the health check monitor
        this.monitorInterval = setInterval(() => {
            this.healthCheckMonitor().catch(() => { });
        }, HEALTH_CHECK_INTERVAL);
    }
    /**
     * Stop the proxy server and its health monitoring.
     *
     * @param timeout - Maximum time in seconds to wait for graceful shutdown (default: 4).
     *                  The server will abort pending connections and background tasks after timeout/2.
     */
    stopServer(timeout = 4) {
        // Stop the monitor
        if (this.monitorInterval !== null) {
            clearInterval(this.monitorInterval);
            this.monitorInterval = null;
        }
        // Reset the health check flag
        this.isHealthCheckRunning = false;
        // Once we stop monitoring, we've lost active management of the server,
        // so we mark it as not running regardless of shutdown success
        this._isRunning = false;
        // Stop the server and re-throw any errors to inform caller
        native.stopWithTimeout(this.port, timeout);
    }
    /**
     * Check if the server is currently running.
     */
    get isRunning() {
        return this._isRunning;
    }
    /**
     * Get the port this server is running on.
     */
    getPort() {
        return this.port;
    }
}
exports.ProxyServer = ProxyServer;
// Singleton instance for backwards compatibility
let globalProxy = null;
function getGlobalProxy(port = 45667) {
    if (globalProxy === null) {
        globalProxy = new ProxyServer(port);
    }
    return globalProxy;
}
/**
 * Run the proxy server in a background thread with health monitoring.
 *
 * This is a backwards-compatible function that uses a global singleton instance.
 * For more control, consider creating your own ProxyServer instance.
 *
 * A background monitor will check the server health every HEALTH_CHECK_INTERVAL ms
 * and automatically restart it if it dies.
 *
 * @param url - The target URL to proxy requests to
 * @param port - The port to listen on (default: 45667)
 */
function runServer(url, port = 45667) {
    // If port is different from current global instance, create a new one
    const proxy = getGlobalProxy(port);
    if (proxy.getPort() !== port) {
        // Stop old instance if running
        if (proxy.isRunning) {
            proxy.stopServer();
        }
        // Create new instance with the requested port
        globalProxy = new ProxyServer(port);
    }
    getGlobalProxy().runServer(url);
}
/**
 * Stop the proxy server and its health monitoring.
 *
 * This is a backwards-compatible function that uses a global singleton instance.
 * For more control, consider creating your own ProxyServer instance.
 *
 * @param timeout - Maximum time in seconds to wait for graceful shutdown (default: 4).
 *                  The server will abort pending connections and background tasks after timeout/2.
 */
function stopServer(timeout = 4) {
    getGlobalProxy().stopServer(timeout);
}
/**
 * Set the current trace context by sending an HTTP request to the running proxy server.
 *
 * This is a backwards-compatible function that uses a global singleton instance.
 * For more control, consider creating your own ProxyServer instance.
 *
 * @param context - The span context containing trace information
 * @throws Error if the HTTP request fails
 */
async function setCurrentTrace(context) {
    return getGlobalProxy().setCurrentTrace(context);
}
// Re-export native functions for direct access if needed
exports.run = native.run, exports.stop = native.stop, exports.stopWithTimeout = native.stopWithTimeout;

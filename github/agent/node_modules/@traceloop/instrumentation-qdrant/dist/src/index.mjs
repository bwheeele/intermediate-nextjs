import { trace, context, SpanStatusCode, SpanKind } from '@opentelemetry/api';
import { InstrumentationBase, InstrumentationNodeModuleDefinition, safeExecuteInTheMiddle } from '@opentelemetry/instrumentation';
import { SpanAttributes, EventAttributes } from '@traceloop/ai-semantic-conventions';

var version = "0.12.0";

/*
 * Copyright Traceloop
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const UPSERT = "upsert";
const DELETE = "delete";
const RETRIEVE = "retrieve";
const SEARCH = "search";
class QdrantInstrumentation extends InstrumentationBase {
    constructor(config = {}) {
        super("@traceloop/instrumentation-qdrant", version, config);
    }
    setConfig(config = {}) {
        super.setConfig(config);
    }
    init() {
        const module = new InstrumentationNodeModuleDefinition("@qdrant/js-client-rest", ["^1.9"], this.wrap.bind(this), this.unwrap.bind(this));
        return module;
    }
    manuallyInstrument(module) {
        this._diag.debug(`Manually patching Qdrant`);
        this.wrap(module);
    }
    wrap(module) {
        this._wrap(module.QdrantClient.prototype, UPSERT, this.wrapperMethod());
        this._wrap(module.QdrantClient.prototype, RETRIEVE, this.wrapperMethod());
        this._wrap(module.QdrantClient.prototype, SEARCH, this.wrapperMethod());
        this._wrap(module.QdrantClient.prototype, DELETE, this.wrapperMethod());
        return module;
    }
    unwrap(module) {
        this._unwrap(module.QdrantClient.prototype, UPSERT);
        this._unwrap(module.QdrantClient.prototype, RETRIEVE);
        this._unwrap(module.QdrantClient.prototype, SEARCH);
        this._unwrap(module.QdrantClient.prototype, DELETE);
        return module;
    }
    wrapperMethod() {
        // eslint-disable-next-line @typescript-eslint/no-this-alias
        const plugin = this;
        // eslint-disable-next-line
        return (original) => {
            return function method(...args) {
                const span = plugin._startSpan({
                    collectionName: args[0],
                    params: args.length > 1 ? args[1] : {},
                    methodName: original.name,
                });
                const execContext = trace.setSpan(context.active(), span);
                const execPromise = safeExecuteInTheMiddle(() => {
                    return context.with(execContext, () => {
                        return original.apply(this, args);
                    });
                }, (e) => {
                    if (e) {
                        plugin._diag.error(`Error in Qdrant instrumentation`, e);
                    }
                });
                const wrappedPromise = execPromise
                    .then((result) => {
                    return new Promise((resolve) => {
                        plugin._endSpan({ methodName: original.name, span, result });
                        resolve(result);
                    });
                })
                    .catch((error) => {
                    return new Promise((_, reject) => {
                        span.setStatus({
                            code: SpanStatusCode.ERROR,
                            message: error.message,
                        });
                        span.end();
                        reject(error);
                    });
                });
                return context.bind(execContext, wrappedPromise);
            };
        };
    }
    _startSpan({ collectionName, params, methodName, }) {
        var _a, _b;
        const spanName = `qdrant.${methodName}`;
        const span = this.tracer.startSpan(spanName, {
            kind: SpanKind.CLIENT,
        });
        span.setAttribute(SpanAttributes.VECTOR_DB_VENDOR, "Qdrant");
        try {
            if (this._config.traceContent) {
                switch (methodName) {
                    case UPSERT:
                        this._setUpsertAttributes(span, collectionName, params);
                        break;
                    case DELETE:
                        this._setDeleteAttributes(span, collectionName, params);
                        break;
                    case RETRIEVE:
                        this._setRetrieveAttributes(span, collectionName, params);
                        break;
                    case SEARCH:
                        this._setSearchAttributes(span, collectionName, params);
                        break;
                    default:
                        break;
                }
            }
        }
        catch (e) {
            this._diag.warn(e);
            (_b = (_a = this._config).exceptionLogger) === null || _b === void 0 ? void 0 : _b.call(_a, e);
        }
        return span;
    }
    _endSpan({ methodName, span, result, }) {
        var _a, _b;
        try {
            switch (methodName) {
                case SEARCH:
                    this._setSearchResultAttributes(span, result);
                    break;
                default:
                    break;
            }
            span.setStatus({ code: SpanStatusCode.OK });
            span.end();
        }
        catch (e) {
            this._diag.debug(e);
            (_b = (_a = this._config).exceptionLogger) === null || _b === void 0 ? void 0 : _b.call(_a, e);
        }
    }
    _setUpsertAttributes(span, collectionName, params) {
        span.setAttribute(SpanAttributes.VECTOR_DB_TABLE_NAME, collectionName);
        if ("batch" in params) {
            span.setAttribute(SpanAttributes.VECTOR_DB_ADD_COUNT, params.batch.ids.length);
        }
        else {
            span.setAttribute(SpanAttributes.VECTOR_DB_ADD_COUNT, params.points.length);
        }
    }
    _setDeleteAttributes(span, collectionName, params) {
        span.setAttribute(SpanAttributes.VECTOR_DB_TABLE_NAME, collectionName);
        if ("filter" in params) {
            span.setAttribute(SpanAttributes.VECTOR_DB_DELETE_SELECTOR, JSON.stringify(params.filter));
        }
        else {
            span.setAttribute(SpanAttributes.VECTOR_DB_DELETE_SELECTOR, JSON.stringify(params.points));
            span.setAttribute(SpanAttributes.VECTOR_DB_DELETE_COUNT, params.points.length);
        }
    }
    _setRetrieveAttributes(span, collectionName, params) {
        span.setAttribute(SpanAttributes.VECTOR_DB_TABLE_NAME, collectionName);
        span.setAttribute(SpanAttributes.VECTOR_DB_GET_SELECTOR, JSON.stringify(params.ids));
        span.setAttribute(SpanAttributes.VECTOR_DB_GET_COUNT, params.ids.length);
        span.setAttribute(SpanAttributes.VECTOR_DB_GET_INCLUDE_METADATA, !!params.with_payload);
        span.setAttribute(SpanAttributes.VECTOR_DB_GET_INCLUDE_VALUES, !!params.with_vector);
    }
    _setSearchAttributes(span, collectionName, params) {
        var _a;
        span.setAttribute(SpanAttributes.VECTOR_DB_TABLE_NAME, collectionName);
        const query_request_event = span.addEvent("qdrant.search.request");
        query_request_event.setAttribute(EventAttributes.VECTOR_DB_QUERY_TOP_K, params.limit);
        query_request_event.setAttribute(EventAttributes.VECTOR_DB_QUERY_INCLUDE_VALUES, !!params.with_vector);
        query_request_event.setAttribute(EventAttributes.VECTOR_DB_QUERY_INCLUDE_METADATA, !!params.with_payload);
        query_request_event.setAttribute(EventAttributes.VECTOR_DB_QUERY_EMBEDDINGS_VECTOR, JSON.stringify(params.vector));
        query_request_event.setAttribute(EventAttributes.VECTOR_DB_QUERY_METADATA_FILTER, JSON.stringify((_a = params.filter) !== null && _a !== void 0 ? _a : {}));
    }
    _setSearchResultAttributes(span, result) {
        const qdrant_result_event = span.addEvent("qdrant.search.result");
        qdrant_result_event.setAttribute(EventAttributes.VECTOR_DB_QUERY_RESULT_MATCHES_LENGTH, result.length);
        for (let i = 0; i < result.length; i++) {
            const match = result[i];
            const search_result_match_event = qdrant_result_event.addEvent(`qdrant.search.result.${i}`);
            search_result_match_event.setAttribute(EventAttributes.VECTOR_DB_QUERY_RESULT_SCORE.replace("{i}", i.toString()), match.score);
            search_result_match_event.setAttribute(EventAttributes.VECTOR_DB_QUERY_RESULT_ID.replace("{i}", i.toString()), match.id);
            search_result_match_event.setAttribute(EventAttributes.VECTOR_DB_QUERY_RESULT_VALUES.replace("{i}", i.toString()), JSON.stringify(match.vector));
            search_result_match_event.setAttribute(EventAttributes.VECTOR_DB_QUERY_RESULT_METADATA.replace("{i}", i.toString()), JSON.stringify(match.payload));
        }
    }
}

export { QdrantInstrumentation };
//# sourceMappingURL=index.mjs.map
